# Salesforce Connection Configuration
SF_LOGIN_URL=https://yourorg.my.salesforce.com
SF_CLIENT_ID=YOUR_CONNECTED_APP_CONSUMER_KEY
SF_CLIENT_SECRET=YOUR_CONNECTED_APP_CONSUMER_SECRET
SF_CALLBACK_URL=http://localhost:3000/oauth/callback

# User Configuration
# Option 1: Specify users directly in environment variable (for a small number of users)
TARGET_USERS=user1@example.com,user2@example.com
# Option 2: Specify path to a CSV file containing user emails (for many users)
USERS_CSV=target-users.csv

# Optional Configuration
# LOG_LEVEL=debug  # Options: debug, info, warn, error
# RISK_CONFIG_FILE=risk-config.json  # Custom risk detection config file
# SCAN_DAYS_BACK=7  # Number of days to look back for login activity (default: all available)
# SCAN_LIMIT=100  # Maximum number of logs to process (default: no limit)